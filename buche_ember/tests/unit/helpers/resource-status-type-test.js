
import { resourceStatusType } from 'ember-quickstart/helpers/resource-status-type';
import { module, test } from 'qunit';

module('Unit | Helper | resource status type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resourceStatusType([42]);
  assert.ok(result);
});

